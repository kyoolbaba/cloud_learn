# --- Local Claude Code settings (machine-specific) ---
.claude/settings.local.json

# --- Secrets & credentials — NEVER commit ---
config.sh
.env
.env.*
*.local
credentials
*.pem
*.key
*.pfx
id_rsa*
.aws/
aws-credentials*

# --- Terraform (state/vars can contain secrets) ---
.terraform/
*.tfstate
*.tfstate.*
*.tfvars

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# --- OS / editor cruft ---
.DS_Store
Thumbs.db

# --- Obsidian local state (keeps shared config, ignores local cache) ---
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.trash/

# --- Nested study repos (have their own git history + remote) ---
production-ready-data-science-code/
